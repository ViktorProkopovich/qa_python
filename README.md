# 📚 Проект Sprint_4 — Класс *BooksCollector*

## 📘 Описание проекта

**BooksCollector** — это класс, который позволяет:
- добавлять книги,
- присваивать им жанры,
- фильтровать книги по жанрам и возрастным категориям,
- добавлять книги в список избранного.

---

## 🧱 Структура класса `BooksCollector`

### Атрибуты:

- `books_genre` — словарь, где ключ — **название книги**, значение — **жанр**.
- `favorites` — список, содержащий **избранные книги**.
- `genre` — список **доступных жанров**.
- `genre_age_rating` — список жанров, которые имеют **возрастное ограничение**.

---

## ⚙️ Методы класса

| Метод | Описание |
|-------|-----------|
| `add_new_book(book_name)` | Добавляет новую книгу без жанра. Название не более 40 символов. Одну и ту же книгу можно добавить только один раз. |
| `set_book_genre(book_name, genre)` | Устанавливает жанр книги, если она есть в `books_genre` и жанр входит в список доступных. |
| `get_book_genre(book_name)` | Возвращает жанр книги по названию. |
| `get_books_with_specific_genre(genre)` | Возвращает список книг с указанным жанром. |
| `get_books_genre()` | Возвращает текущий словарь `books_genre`. |
| `get_books_for_children()` | Возвращает список книг без возрастного рейтинга. |
| `add_book_in_favorites(book_name)` | Добавляет книгу в избранное (если она есть в `books_genre` и ещё не добавлена). |
| `delete_book_from_favorites(book_name)` | Удаляет книгу из избранного, если она там есть. |
| `get_list_of_favorites_books()` | Возвращает список избранных книг. |

---

## 🧪 Тестируемые методы

| Тест | Проверка |
|------|-----------|
| `test_add_new_book_length_more_40` | Проверяет ограничение длины названия книги (≤ 40 символов). |
| `test_set_book_genre` | Проверяет установку жанра книге. |
| `test_get_book_genre` | Проверяет получение жанра по названию книги. |
| `test_get_books_with_specific_genre` | Проверяет фильтрацию книг по жанру. |
| `test_get_books_genre` | Проверяет возврат словаря `books_genre`. |
| `test_get_books_for_children` | Проверяет, что возвращаются только книги без возрастного рейтинга. |
| `test_add_book_in_favorites` | Проверяет добавление книги в избранное. |
| `test_add_duplicate_book_in_favorites` | Проверяет, что повторно добавить книгу в избранное нельзя. |
| `test_delete_book_from_favorites` | Проверяет удаление книги из избранного. |
| `test_get_list_of_favorites_books` | Проверяет получение списка избранных книг. |

---

## 🧩 Используемые технологии

- **Python 3.10+**
- **Pytest** — для автоматического тестирования

---

## 🚀 Пример использования

```python
from books_collector import BooksCollector

collector = BooksCollector()
collector.add_new_book("Гарри Поттер")
collector.set_book_genre("Гарри Поттер", "Фэнтези")
collector.add_book_in_favorites("Гарри Поттер")

print(collector.get_books_genre())
# {'Гарри Поттер': 'Фэнтези'}

print(collector.get_list_of_favorites_books())
# ['Гарри Поттер']

👨‍💻 Автор

Виктор Прокопович
📦 GitHub — ViktorProkopovich


---
