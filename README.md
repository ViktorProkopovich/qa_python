# 📚 Проект Sprint_4 — Класс *BooksCollector*

## 📘 Описание проекта

Проект представляет собой класс **BooksCollector**, который позволяет:
- добавлять книги,
- присваивать им жанры,
- фильтровать книги по жанрам или возрастным категориям,
- добавлять книги в избранное.

Цель проекта — реализовать и протестировать логику работы с коллекцией книг с помощью модульных тестов **Pytest**.

---

## 🧩 Функциональность класса BooksCollector

### 🗂️ Основные структуры данных:
- **`books_genre`** — словарь, где хранится пара *Название книги : Жанр книги*  
- **`favorites`** — список избранных книг  
- **`genre`** — список доступных жанров  
- **`genre_age_rating`** — список жанров с возрастным ограничением  

---

### ⚙️ Методы класса:

| Метод | Описание |
|--------|-----------|
| `add_new_book(name)` | Добавляет новую книгу без жанра (название ≤ 40 символов). Одну и ту же книгу нельзя добавить дважды. |
| `set_book_genre(name, genre)` | Устанавливает жанр книги, если она есть в словаре и жанр допустим. |
| `get_book_genre(name)` | Возвращает жанр книги по названию. |
| `get_books_with_specific_genre(genre)` | Возвращает список книг определённого жанра. |
| `get_books_genre()` | Возвращает текущий словарь книг и их жанров. |
| `get_books_for_children()` | Возвращает книги, подходящие детям (жанры без возрастных ограничений). |
| `add_book_in_favorites(name)` | Добавляет книгу в избранное (если она есть в словаре и не добавлена ранее). |
| `delete_book_from_favorites(name)` | Удаляет книгу из избранного, если она там есть. |
| `get_list_of_favorites_books()` | Возвращает список избранных книг. |

---

## 🧪 Тесты

Тесты проверяют корректность работы всех методов класса **BooksCollector**.

| Тест | Проверка |
|------|-----------|
| `test_add_new_book_length_more_40` | Книга с названием > 40 символов не добавляется |
| `test_set_book_genre` | Устанавливается корректный жанр книге |
| `test_get_book_genre` | Возвращает установленный жанр |
| `test_get_books_with_specific_genre` | Возвращает список книг конкретного жанра |
| `test_get_books_genre` | Возвращает весь словарь книг и жанров |
| `test_get_books_for_children` | Возвращает книги, подходящие детям |
| `test_add_book_in_favorites` | Добавляет книгу в избранное |
| `test_add_duplicate_book_in_favorites` | Не добавляет книгу повторно в избранное |
| `test_delete_book_from_favorite` | Удаляет книгу из избранного |
| `test_get_list_of_favorites_books` | Возвращает список избранных книг |

---

## 📁 Структура проекта

~~~text
Sprint_4/
│
├── tests/                     # Тесты Pytest
│   └── test_books_collector.py
│
├── books_collector.py         # Основной класс BooksCollector
├── conftest.py                # Фикстуры Pytest
├── data.py                    # Тестовые данные
├── .gitignore                 # Исключаемые файлы/папки
├── README.md                  # Описание проекта
└── requirements.txt           # Зависимости
~~~

---

## ⚙️ Запуск тестов

### 1️⃣ Клонировать репозиторий

~~~bash
git clone https://github.com/ViktorProkopovich/Sprint_4.git
cd Sprint_4
~~~

---

### 2️⃣ Создать и активировать виртуальное окружение

~~~bash
python -m venv venv
~~~

**Активация:**

🪟 **Windows**
~~~bash
venv\Scripts\activate
~~~

🐧 **MacOS / Linux**
~~~bash
source venv/bin/activate
~~~

---

### 3️⃣ Установить зависимости

~~~bash
pip install -r requirements.txt
~~~

---

### 4️⃣ Запустить тесты

~~~bash
pytest -v
~~~

---

## 🧠 Что покрывают тесты

| Категория | Проверки |
|------------|-----------|
| **Добавление книг** | Корректное добавление, ограничение по длине, отсутствие дубликатов |
| **Жанры** | Установка, фильтрация, возрастные ограничения |
| **Избранное** | Добавление, удаление, запрет дубликатов |
| **Вывод данных** | Возврат словаря, фильтрация, списки по жанрам и избранному |

---

## 🧱 Используемые технологии

- 🐍 **Python 3.10+**  
- 🧪 **Pytest**  
- 🧱 **Объектно-ориентированное программирование (ООП)**  
- 🧩 **TDD-подход** (разработка через тестирование)

---

## 👨‍💻 Автор

**Виктор Прокопович**  
📦 [GitHub — ViktorProkopovich](https://github.com/ViktorProkopovich)
